Audio files are optional — the AudioManager synthesizes tones as fallback.
If you want to use pre-recorded files, place them here:
  tone_high.mp3     (800 Hz sine, 150ms)
  tone_low.mp3      (400 Hz sine, 150ms)
  tone_distractor.mp3 (600 Hz sine, 150ms)
